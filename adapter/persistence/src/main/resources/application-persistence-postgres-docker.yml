spring:
  config:
    import:
      classpath:application-persistence.yml,classpath:application-persistence-postgres.yml
  datasource:
    url: "jdbc:postgresql://postgres:5432/test_db"
    username: sa
    password: sa
